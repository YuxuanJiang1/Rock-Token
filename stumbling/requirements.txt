# Pinned + [all] extra to match docker/Dockerfile.sgl059.torch291.cu128, the only
# environment actually validated by the KDFlow authors. A bare "sglang" (no [all],
# no version pin) does not reliably pull in sgl-kernel, causing
# "ModuleNotFoundError: No module named 'sgl_kernel'" at runtime.
sglang[all]==0.5.9
torch>=2.4
transformers
numpy
ray>=2.0
sglang_router
datasets
tqdm
requests
safetensors
huggingface_hub
torchdata
peft
wandb   
triton
ring_flash_attn

# install flash_attn after torch installation:
# pip install flash_attn==2.8.3 --no-build-isolation
